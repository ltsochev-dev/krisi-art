# Dependencies are installed inside the image by the deps stage — copying the
# host's node_modules over them breaks pnpm's deps check and the native builds.
node_modules
.pnp
.pnp.js

# Build output
.next
out
build
*.tsbuildinfo

# Local data and secrets
.env
.env*.local
database
media

# Test artifacts
coverage
test-results
playwright-report
blob-report
playwright/.cache

# VCS and tooling
.git
.gitignore
.vscode
.idea
.claude
Dockerfile
docker-compose.yml
npm-debug.log*
.DS_Store
